pytest>=8
ruff>=0.6
pandas>=2.2  # `to_dataframe` uniquement — commodité d exploration, JAMAIS le chemin d execution
mypy>=1.11   # typage strict — voir [tool.mypy] : `app/replay` et `app/datasource/replay.py`
