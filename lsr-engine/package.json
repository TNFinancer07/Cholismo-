{
  "name": "lsr-engine",
  "version": "1.2.0",
  "description": "LSR v1.2 — Liquidity Sweep Reversion engine (Scanner Phase 0 + order flow gates + geometry + RiskSizer) for Cholismo",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
